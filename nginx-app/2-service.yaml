apiversion: v1
kind: service
metadata:
  name: web-app
  namespace: appcd
  labels:
    app: nginx-app
spec:
  selector:
    app: nginx-app
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetport: 80
